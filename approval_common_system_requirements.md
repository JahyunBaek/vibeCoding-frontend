# 공통 결재 시스템 요구사항 정의서

## 1. 문서 목적
본 문서는 **React 19 + Spring Boot 3 + MyBatis + PostgreSQL + JDK 24** 기반의 SaaS형 공통시스템에서 사용할 **멀티테넌트 결재(전자결재/승인 워크플로우) 기능**을 설계·구현하기 위한 요구사항을 정리한 것이다.

이 문서는 Claude에게 그대로 전달하여,
- 화면/백엔드/API/DB 설계
- 권한 정책 정의
- 공통 컴포넌트 설계
- 결재선 관리 및 결재 진행 로직 구현

을 요청할 수 있도록 작성되었다.

---

## 2. 시스템/기술 전제
- Frontend: **React 19**
- Backend: **Spring Boot 3**
- ORM/SQL Mapper: **MyBatis**
- Database: **PostgreSQL**
- JDK: **24**
- Architecture: **SaaS 멀티테넌트 구조**
- 모든 결재 관련 데이터는 반드시 **tenant_id** 기준으로 분리되어야 한다.

### 멀티테넌시 원칙
- 모든 마스터/설정/템플릿/결재문서/결재이력/결재선 데이터는 `tenant_id`를 포함해야 한다.
- 타 테넌트 데이터는 절대 조회/수정되지 않아야 한다.
- 관리자 기능 또한 테넌트 범위 내 관리자만 접근 가능해야 한다.

---

## 3. 구현 목표
공통시스템에서 여러 업무 기능이 동일한 결재 엔진을 사용할 수 있도록 아래와 같은 구조를 만든다.

### 핵심 개념
1. 각 업무 기능은 **결재 기능 코드(approval_code)** 로 식별된다.
2. 각 결재 기능 코드는 결재 시 사용할 정책을 가진다.
3. 결재는 다음 부서 개념을 사용할 수 있다.
   - **신청부서**
   - **주관부서**
4. 어떤 결재 기능은 신청부서만 사용하고,
5. 어떤 결재 기능은 신청부서 + 주관부서를 함께 사용한다.
6. 주관부서를 사용하는 경우, **관리자가 사전에 주관부서를 지정**할 수 있어야 한다.
7. 각 부서 결재는 **개인 지정 방식이 아니라 그룹결재 방식**을 지원해야 한다.
   - 즉, 해당 부서의 결재 대상자로 지정된 사용자 중 **누구나** 결재 가능해야 한다.
8. 각 사용자는 특정 결재 기능 코드별로 자신의 **신청부서 결재선 양식(템플릿)** 을 여러 개 저장할 수 있어야 한다.
9. 사용자는 각 기능 코드별 결재선 양식 중 **기본값(default)** 을 하나 지정할 수 있어야 한다.
10. 결재 현황은 특정 업무 화면에 종속되지 않고 **어느 화면에서든 공통적으로 조회 가능**해야 한다.

---

## 4. 용어 정의

### 4.1 결재 기능 코드 (approval_code)
각 업무 기능이 결재를 호출할 때 사용하는 고유 코드.
예)
- `SR_APPROVAL`
- `CHANGE_APPROVAL`
- `PURCHASE_APPROVAL`
- `ACCESS_REQUEST_APPROVAL`

이 코드를 기반으로 결재 정책, 기본 부서 사용 여부, 주관부서 사용 여부, 결재선 템플릿 범위가 결정된다.

### 4.2 신청부서
결재를 요청하는 사용자가 소속된 부서.
결재 요청자의 기본 소속 부서이며, 일반적으로 결재 시작점이 된다.

### 4.3 주관부서
특정 결재 기능에서 추가로 필요한 관리/검토/주관 역할의 부서.
이 부서는 결재 기능 코드별로 관리자가 사전에 지정할 수 있다.

### 4.4 그룹결재
해당 결재 단계가 특정 개인 1명이 아니라 **특정 부서 또는 그룹에 열려 있는 상태**를 의미한다.
해당 단계의 결재 권한을 가진 사람이라면 **누구나 승인/반려 처리 가능**해야 한다.

### 4.5 결재선 양식(템플릿)
사용자가 특정 결재 기능 코드에 대해 미리 저장해 두는 개인용 신청부서 결재선 구성.
한 사용자는 하나의 기능 코드에 대해 여러 개 양식을 저장할 수 있으며, 그 중 하나를 기본값으로 지정할 수 있다.

---

## 5. 요구사항 요약

### 5.1 관리자 설정 기능
관리자는 결재 기능 코드별로 다음을 설정할 수 있어야 한다.
- 결재 기능 코드
- 결재 명칭
- 설명
- 사용 여부
- 신청부서 사용 여부
- 주관부서 사용 여부
- 주관부서 기본 지정값
- 결재 단계 정책
- 그룹결재 허용 여부
- 결재 상태 표시 여부
- 개인 결재선 템플릿 허용 여부

### 5.2 사용자 기능
일반 사용자는 다음이 가능해야 한다.
- 본인이 사용할 수 있는 결재 기능 코드 목록 조회
- 특정 기능 코드에 대한 **개인 결재선 양식** 등록/수정/삭제
- 동일 기능 코드 내 여러 개 양식 저장
- 기본 양식 지정
- 결재 요청 시 저장된 양식 불러오기
- 결재 요청 문서의 진행 상태 조회
- 본인이 신청한 문서 / 본인이 결재 가능한 문서 / 본인이 결재한 문서 조회

### 5.3 공통 결재 팝업/화면
업무 화면에서 특정 `approval_code` 로 결재 창을 호출하면 다음이 자동 세팅되어야 한다.
- 해당 코드에 설정된 결재 정책 조회
- 신청부서 사용 여부 반영
- 주관부서 사용 여부 반영
- 주관부서 기본값 자동 세팅
- 사용자의 기본 결재선 양식 자동 조회
- 필요 시 양식 선택 가능
- 결재 요청 전 최종 결재선 검토 가능

---

## 6. 상세 요구사항

## 6.1 결재 정책 마스터 관리
관리자 전용 화면에서 결재 기능 코드를 관리한다.

### 관리자 화면에서 필요한 항목
- 결재 기능 코드
- 결재 명칭
- 결재 설명
- 활성 여부
- 정렬순서
- 신청부서 사용 여부 (`Y/N`)
- 주관부서 사용 여부 (`Y/N`)
- 주관부서 기본 지정 가능 여부
- 기본 주관부서
- 그룹결재 사용 여부
- 개인 결재선 양식 사용 여부
- 비고

### 제약사항
- 결재 기능 코드는 테넌트 내에서 유일해야 한다.
- 비활성화된 결재 기능 코드는 신규 결재 요청에 사용할 수 없어야 한다.
- 기존 결재 이력에는 영향이 없어야 한다.

---

## 6.2 주관부서 지정 정책
주관부서를 사용하는 기능 코드의 경우, 관리자가 사전에 주관부서를 지정할 수 있어야 한다.

### 요구사항
- 하나의 기능 코드에 대해 기본 주관부서 1개 또는 여러 개 정책을 둘 수 있는지 확장 가능하게 설계한다.
- 초기 구현은 **기본 주관부서 1개** 기준으로 단순하게 시작해도 좋다.
- 향후 확장을 고려하여 테이블/코드는 다중 주관부서가 가능하도록 유연하게 설계한다.

### 처리 방식
- 사용자가 결재 팝업을 열면, 주관부서 사용 기능인 경우 기본 주관부서를 자동 세팅한다.
- 필요 시 사용자가 변경 가능한지 여부는 정책으로 분리 가능하게 설계한다.

---

## 6.3 그룹결재
부서 결재 단계는 개인 지정 결재가 아니라 그룹결재가 가능해야 한다.

### 의미
예를 들어 신청부서 1차 승인 단계가 있다면,
- 신청부서의 팀장 1명만 결재 가능한 것이 아니라
- 신청부서 승인 권한을 가진 사용자들 중 **누구나** 처리 가능해야 한다.

주관부서도 동일하다.

### 요구사항
- 결재 단계별 승인 대상은 `user_id` 단위가 아닌 `department_id`, `role`, `group`, `approval_authority` 등의 조합으로 표현 가능해야 한다.
- 해당 단계를 처리할 수 있는 대상자가 여러 명일 수 있다.
- 이 중 한 명이 승인하면 해당 단계는 완료 처리된다.
- 이미 다른 사람이 승인한 뒤에는 나머지 사람은 중복 승인할 수 없어야 한다.
- 동시성 충돌 방지 로직이 필요하다.

### 예시
- 신청부서 결재자 그룹: 신청자의 소속 부서 내 `APPROVER` 권한 사용자 전원
- 주관부서 결재자 그룹: 지정된 주관부서 내 `APPROVER` 권한 사용자 전원

---

## 6.4 개인 결재선 양식 관리
사용자는 특정 결재 기능 코드에 대해서만 자신의 결재선 양식을 저장할 수 있어야 한다.

### 요구사항
- 양식은 사용자 개인 소유이다.
- 양식은 `approval_code` 별로 분리된다.
- 다른 코드에서는 다른 코드용 양식이 보이면 안 된다.
- 한 사용자는 한 코드에 대해 여러 개 양식을 저장할 수 있다.
- 그 중 1개를 기본 양식으로 설정할 수 있다.
- 기본 양식은 코드별 1개만 가능해야 한다.
- 양식명(예: 기본결재선, 긴급용, 보안검토포함 등)을 지정할 수 있어야 한다.
- 사용자는 본인의 양식만 조회/수정/삭제 가능해야 한다.

### 양식에 포함될 수 있는 정보
- 신청부서 결재 단계 구성
- 단계 순서
- 단계명
- 승인 타입(승인/합의/검토 등 향후 확장 가능)
- 그룹결재 대상 정보
- 참조자/열람자 (선택)

### 권장사항
초기 구현은 복잡도를 낮추기 위해,
- 신청부서 결재선 중심으로 템플릿을 저장하고
- 주관부서는 관리자 기본설정 기반으로 자동 주입되도록 설계하는 것이 좋다.

---

## 6.5 결재 요청 처리
어떤 업무 화면에서든 특정 `approval_code` 로 결재 요청을 시작할 수 있어야 한다.

### 호출 방식 예시
업무 시스템에서 공통 결재 모듈 호출 시 다음 정보가 전달된다.
- `tenant_id`
- `approval_code`
- `document_type`
- `document_id`
- `title`
- `requester_user_id`
- `requester_department_id`
- 기타 업무 데이터

### 결재 팝업 초기화 로직
1. `approval_code` 로 결재 정책 조회
2. 신청부서 사용 여부 확인
3. 주관부서 사용 여부 확인
4. 주관부서 기본값 세팅
5. 사용자 기본 결재선 양식 조회
6. 저장된 양식 목록 제공
7. 최종 결재선 생성
8. 사용자가 확인 후 결재 요청

### 결재 요청 시 저장되어야 할 정보
- 결재문서 기본정보
- 업무 원문서 참조 정보(document_type, document_id)
- 요청자 정보
- 신청부서 정보
- 주관부서 정보
- 결재 단계 목록
- 현재 진행 단계
- 결재 상태
- 생성일시 / 수정일시

---

## 6.6 공통 결재 현황 조회
결재는 특정 업무 화면에 종속되지 않고 공통적으로 조회 가능해야 한다.

### 반드시 필요한 조회 화면
1. **내가 신청한 결재**
2. **내가 결재할 수 있는 결재**
3. **내가 처리한 결재**
4. **참조/열람 결재** (필요 시)
5. **전체 결재 현황** (관리자 또는 권한자)

### 조회 조건 예시
- 결재 기능 코드
- 제목
- 문서번호
- 요청자
- 신청부서
- 주관부서
- 상태(임시저장/진행중/승인/반려/회수/취소)
- 요청일자 기간
- 완료일자 기간

### 상태 예시
- DRAFT
- REQUESTED
- IN_PROGRESS
- APPROVED
- REJECTED
- CANCELED
- WITHDRAWN

### 공통 UI 요구사항
- 어떤 화면에서든 상단 헤더나 별도 메뉴를 통해 진입 가능해야 한다.
- 업무 원문서로 돌아갈 수 있는 링크 또는 식별자 제공이 필요하다.
- 현재 단계, 다음 단계, 처리자, 처리일시 등을 한눈에 보여줘야 한다.

---

## 6.7 권한 요구사항

### 관리자 전용 화면
다음 화면은 기본적으로 관리자만 접근 가능해야 한다.
- 결재 기능 코드 관리
- 결재 정책 관리
- 주관부서 기본 설정
- 결재 권한 그룹 관리
- 전체 결재 현황 관리

### 개인 사용자 화면
다음 화면은 일반 사용자도 접근 가능해야 한다.
- 내 결재선 양식 관리
- 내 신청 결재 조회
- 내 처리 대상 결재 조회
- 내 처리 이력 조회

### 권한 체크 기준
- `tenant_id`
- 사용자 역할(Role)
- 관리자 여부
- 결재 권한 보유 여부
- 부서 소속

---

## 7. 추천 화면 구성

## 7.1 관리자 화면
### 1) 결재 기능 코드 관리
- 목록 조회
- 신규 등록
- 수정
- 비활성화

컬럼 예시:
- 결재코드
- 결재명
- 신청부서 사용
- 주관부서 사용
- 기본 주관부서
- 그룹결재 사용
- 사용 여부

### 2) 결재 권한 관리
- 부서별 결재 가능 권한 사용자 지정
- 역할/직급/권한그룹 기준 관리 가능하도록 확장 고려

### 3) 전체 결재 현황
- 테넌트 전체 문서 조회
- 상태/기능코드/부서별 필터

---

## 7.2 사용자 화면
### 1) 내 결재선 양식 관리
- 기능 코드 선택
- 내 양식 목록 조회
- 새 양식 저장
- 수정/삭제
- 기본 양식 지정

### 2) 공통 결재 팝업
- 결재 문서 제목
- 신청부서 표시
- 주관부서 표시
- 양식 선택
- 결재선 미리보기
- 상신 버튼

### 3) 내 결재함
- 내가 신청한 문서
- 내가 결재할 수 있는 문서
- 내가 처리한 문서

---

## 8. 추천 데이터 모델(개념 수준)
Claude에게 아래 방향으로 설계 요청.

## 8.1 마스터/설정 계층

### approval_definition
결재 기능 코드 마스터
- id
- tenant_id
- approval_code
- approval_name
- description
- use_request_department
- use_supervising_department
- default_supervising_department_id
- use_group_approval
- use_personal_line_template
- active_yn
- sort_order
- created_by
- created_at
- updated_by
- updated_at

### approval_authority_rule
결재 가능 권한 정의
- id
- tenant_id
- approval_code
- target_department_id
- target_role_code / authority_code
- step_type
- active_yn
- created_at
- updated_at

---

## 8.2 개인 결재선 양식 계층

### approval_line_template
개인 결재선 템플릿 헤더
- id
- tenant_id
- approval_code
- owner_user_id
- template_name
- default_yn
- active_yn
- created_at
- updated_at

### approval_line_template_step
개인 결재선 템플릿 상세 단계
- id
- template_id
- step_order
- step_name
- approval_type
- target_department_type (`REQUEST`, `SUPERVISING`, `CUSTOM`)
- target_department_id
- target_role_code / authority_code
- group_approval_yn
- required_yn

---

## 8.3 실제 결재 문서 계층

### approval_document
실제 결재 인스턴스
- id
- tenant_id
- approval_code
- document_type
- document_id
- title
- requester_user_id
- requester_department_id
- supervising_department_id
- status
- current_step_order
- requested_at
- completed_at
- withdrawn_at
- canceled_at
- created_at
- updated_at

### approval_document_step
실제 결재 단계
- id
- approval_document_id
- step_order
- step_name
- approval_type
- target_department_id
- target_role_code / authority_code
- group_approval_yn
- status
- acted_by_user_id
- acted_at
- comment

### approval_document_actor
해당 단계 처리 가능 사용자 스냅샷(선택)
- id
- approval_document_step_id
- actor_user_id
- actor_department_id
- actor_role_code
- action_status

### approval_history
결재 이력
- id
- tenant_id
- approval_document_id
- step_id
- action_type
- action_by
- action_comment
- action_at
- before_status
- after_status

---

## 9. 주요 비즈니스 규칙

1. 모든 데이터는 `tenant_id` 단위로 분리한다.
2. 결재 기능 코드는 테넌트 내 유일해야 한다.
3. 개인 결재선 양식은 사용자 + 기능 코드 범위에서만 유효하다.
4. 기본 양식은 사용자 + 기능 코드당 1개만 허용한다.
5. 주관부서는 결재 정책에 따라 자동 세팅된다.
6. 그룹결재 단계는 대상자 중 1명이 처리하면 완료된다.
7. 이미 완료된 단계는 중복 처리할 수 없다.
8. 반려 시 이전 단계로 되돌릴지, 요청자에게 즉시 종료할지는 정책화 가능하게 설계한다.
9. 결재 회수/취소 가능 조건은 명확히 정의해야 한다.
10. 업무 원문서 삭제/상태변경 시 결재문서와의 정합성을 유지해야 한다.

---

## 10. Claude에게 요청하고 싶은 구현 범위
아래 내용을 포함한 형태로 설계 및 예시 코드를 요청하고 싶다.

### 10.1 백엔드
- 도메인 설계
- 테이블 DDL 설계(PostgreSQL)
- MyBatis Mapper XML 및 인터페이스
- Service 계층 설계
- 트랜잭션 처리
- 그룹결재 동시성 제어
- 결재 요청/승인/반려/회수 API 설계
- 멀티테넌트 필터링 전략
- 감사 로그/이력 저장 방식

### 10.2 프론트엔드
- 관리자 화면 구조
- 개인 결재선 양식 관리 화면
- 공통 결재 팝업 컴포넌트
- 공통 결재 현황 목록/상세 화면
- React Query 또는 상태관리 방식 제안
- 재사용 가능한 컴포넌트 구조 제안

### 10.3 공통 아키텍처
- 어느 업무 화면에서도 재사용 가능한 결재 호출 방식
- `approval_code` 기반 설정 로딩 구조
- 원문서와 결재문서 연결 구조
- 확장 가능한 승인 단계 모델

---

## 11. Claude에게 추가로 꼭 검토받고 싶은 포인트

### 11.1 동시성
그룹결재에서 여러 사용자가 동시에 승인하려고 할 수 있으므로,
- 낙관적 락 / 비관적 락 / 상태조건 UPDATE
- 중복 승인 방지
- 이미 처리된 단계 재처리 방지

전략을 제안해 달라.

### 11.2 확장성
향후 아래 기능 확장이 가능하도록 설계해 달라.
- 합의/검토/참조 단계
- 병렬 결재
- 조건부 결재
- 위임 결재
- 부재 시 대결
- 결재 알림
- SLA/지연 경고
- 모바일 화면 대응

### 11.3 보안
- 테넌트 분리 보장
- 사용자 권한 검증
- 관리자 화면 접근 제한
- 타인의 템플릿 접근 차단

---

## 12. 보완 제안사항
아래 항목은 현재 요구사항에 없지만 실무상 거의 필요해질 가능성이 높으므로 함께 설계 고려를 요청한다.

### 12.1 결재 의견(Comment)
승인/반려 시 의견 입력 기능.

### 12.2 첨부파일
결재문서 또는 결재의견에 첨부파일 필요 여부 검토.

### 12.3 알림
- 결재 요청 시 알림
- 내 차례 도래 시 알림
- 반려/승인 완료 시 알림

### 12.4 회수/재상신
- 아직 다음 단계 미처리 시 요청자가 회수 가능할지
- 반려 후 재상신 허용 여부

### 12.5 문서번호
결재문서 전용 번호 체계 필요 여부.

### 12.6 감사/Audit
누가 언제 어떤 상태를 변경했는지 추적 가능해야 한다.

---

## 13. Claude에게 전달할 최종 요청문
아래 요구사항을 만족하는 공통 결재 시스템을 설계해 주세요.

- 기술스택: React 19, Spring Boot 3, MyBatis, PostgreSQL, JDK 24
- SaaS 멀티테넌트 구조이며 모든 결재 데이터는 tenant_id 기준으로 분리되어야 합니다.
- 결재는 approval_code(기능 코드) 기반으로 동작해야 합니다.
- 각 approval_code 별로 결재 명칭/설명/사용여부/신청부서 사용 여부/주관부서 사용 여부를 설정할 수 있어야 합니다.
- 주관부서를 사용하는 경우 관리자가 기본 주관부서를 사전에 지정할 수 있어야 합니다.
- 부서 결재는 그룹결재를 지원해야 하며, 해당 부서의 권한자 누구나 승인할 수 있어야 합니다.
- 사용자는 approval_code 별로 자신의 신청부서 결재선 양식을 여러 개 저장할 수 있어야 하며, 기본 양식을 하나 지정할 수 있어야 합니다.
- 저장된 결재선 양식은 해당 approval_code 에서만 보여야 합니다.
- 어떤 업무 화면에서든 approval_code 로 공통 결재 팝업을 호출하면, 사전 설정된 결재 정책과 사용자의 기본 결재선 양식이 자동 세팅되어야 합니다.
- 결재 현황은 공통 메뉴/공통 화면에서 조회 가능해야 합니다.
- 결재 기능 코드 및 정책 관리 화면은 관리자만 접근 가능해야 합니다.
- 개인 결재선 양식 관리 화면은 일반 사용자도 본인 것만 접근 가능해야 합니다.

다음 산출물을 주세요.
1. 추천 아키텍처
2. DB 테이블 설계 및 PostgreSQL DDL
3. Spring Boot + MyBatis 백엔드 설계
4. React 화면 설계
5. 주요 API 목록
6. 그룹결재 동시성 처리 전략
7. 향후 확장 포인트
8. 예시 코드(핵심부)

---

## 14. 추가 오픈 이슈
아래 항목은 구현 전 정책 결정을 권장한다.

1. 반려 시 프로세스 종료인지, 이전 단계 복귀인지
2. 결재 회수 가능 시점
3. 결재선 수동 변경 허용 범위
4. 주관부서 사용자 변경 가능 여부
5. 병렬 결재 필요 여부
6. 합의/검토/참조 단계 필요 여부
7. 대결/위임 필요 여부
8. 결재 알림 채널(이메일, 사내알림, SMS 등)
9. 문서 보존기간/이력 보존정책
10. 조직개편 시 부서/권한 재계산 정책

---

## 15. 기대 구현 방향
복잡한 전자결재 시스템 전체를 한 번에 만들기보다,
우선 아래 MVP 순서로 구현하는 방향을 권장한다.

### 1차(MVP)
- approval_code 마스터 관리
- 신청부서/주관부서 사용 여부 설정
- 기본 주관부서 설정
- 개인 결재선 양식 관리
- 결재 요청/승인/반려
- 공통 결재 현황 조회
- 그룹결재 1단계 구현

### 2차
- 알림
- 참조/열람
- 회수/재상신
- 관리자 전체 모니터링 강화

### 3차
- 위임/대결
- 병렬 결재
- 조건부 결재
- SLA
- 통계/리포트

---

이 문서를 기반으로 Claude가 설계/코드 생성을 할 수 있도록 구체적이고 실무적으로 답변해 주면 좋겠습니다.

